# Compatibility alias for the canonical post-enrollment multi-user runtime.
# Keep one production service contract to prevent image, secret, and auth drift.
name: stock-insight

include:
  - path: docker-compose.prod-db-auth.yml
