# 남은 일 전수 — 2026-08-05

"거시 상관 다음에 뭐가 남았나" 에 대한 답. **두 가지가 아니라 열아홉 가지다.**

모든 수치는 2026-08-05 00:00~01:30 KST 라이브 DB 관측이다. 기억에서 옮겨 적은
것은 없고, 근거가 없는 항목은 "미확인" 이라고 적었다.

우선순위가 아니라 **성격별**로 묶었다. 성격이 다르면 푸는 방법도 다르고, 한
가지 빨강으로 칠하면 "다 고장났다" 로 읽히기 때문이다.

---

## A. 배포·운영 — 지금 당장 걸리는 것

### A1. 미배포 커밋 17개 · DB 가 앱보다 앞서 있다

프로덕션 이미지 핀은 `e989889`. 그 뒤 17커밋이 미배포다. DB 는 마이그레이션
**066 까지 반영**됐고(오늘 적용), 앱 이미지는 그 전이다.

이번 변경들은 수집·분석 잡 쪽이라 화면 영향이 없다. 다만 잡은 **워킹 트리를
그대로 실행**하므로(A2 참조) 이미 살아 있다.

### A2. ~~⚠ 리더 권한 다이제스트 2개가 어긋나 있다~~ — **해결 (2026-08-05)**

증명하고 재핀했다. 리더 역할로 배열을 읽기 전용 재계산해
`analytics.macro_series_topic` 한 항목만 빼니 두 다이제스트 모두 이전 핀을
**바이트 단위로 재현**했다(relation_privileges 는 773항목 중 1항목). 즉 드리프트는
전부 065 의 것이고, 066 은 적용 전후 값이 동일해 무관함이 증명됐다.

재핀 후 `repin-live-database-digests.mjs` 가 "일치 — 재핀 불필요" 를 낸다.
가드 테스트 픽스처도 함께 옮겼다(안 옮기면 `pnpm test` 가 깨진다 — 깨뜨려서 알았다).

**아래는 해결 전 기록이다.**

```
stock_insight_app_reader
  ≠ relation_privileges_digest   pinned c0d21416… / live f3a18fad…
  ≠ rls_contract_digest          pinned b2566392… / live 71956bd4…
stock_insight_app_writer         전부 일치
```

`live-database-guard.ts` 가 이 다이제스트로 부팅을 막는다. **어긋난 채 배포하면
`live database verification failed for stock_insight_app_reader` 만 남기고
크래시루프에 빠진다** — 2026-08-03 에 45분 내려간 것과 같은 방식이다.

원인은 **마이그레이션 065**(`analytics.macro_series_topic` 테이블 생성)로 보인다.
오늘 066 적용 전후로 다이제스트를 재 봤고 **바이트 단위로 동일**했으므로 066 은
무관하다는 것이 증명됐다. 065 가 원인이라는 것까지는 정황이고, 옮겨 적기 전에
라이브에서 읽기 전용으로 배열을 재계산해 `macro_series_topic` 을 뺀 값이 이전 핀과
일치하는지 확인해야 한다(2026-08-04 에 062·063 을 그 방법으로 특정했다).

**배포 전 반드시 해결.** 이게 이 목록에서 가장 급하다.

### A3. ~~`pnpm verify:release` 시각 게이트~~ — **해결 (2026-08-05)**

`market-graphite` 의 `--color-text-tertiary` 대비 미달로 10개 라우트가 8/8 실패하던
것을 고쳤다. 재측정: **24 passed · 0 failed**. 자세한 것은 아래 F1.

---

## B. 인과 술어 — 격리 3,312건

경로가 밟는 술어는 이제 넷이지만(`SAME_ETF_BASKET`·`PRODUCT_SIMILARITY`·
`CLASSIFIED_AS`·`MACRO_COMOVEMENT`) **인과는 여전히 하나도 없다.**

```
COMMON_OWNER   1,062      SAME_THEME     410      SUPPLY_CHAIN  169
AFFECTS          616      PEER_OF        174      ROLLS_UP      129
SAME_INDUSTRY    418                              EXPOSES       107
```

### B1. 구조적 2,151건 — 소급 불가, 차단 유지 권장

마이그레이션 023 이 백필했고 **출처 메타데이터조차 없다.** 어떤 증거 종류로도
소급할 수 없다. 푸는 유일한 길은 증거 규칙 완화이고 그건 게이트 무력화다.

### B2. 통계적 1,151건 — 두 갈래이고 둘 다 이미 답이 나왔다

- `COMMON_OWNER` 1,062 — 담당 빌더 `ownership` 에 호출자가 없다. **배선해도
  이득 0** 으로 측정됐다(2026-08-04): 401쌍이 전부 이미 `SAME_ETF_BASKET` 이
  잇는 쌍이고 종목 100개가 전부 이미 그래프에 있다. 구현했다가 되돌렸다.
- `NEWS_COMENTION` 68 — 정책이 `promotionEligible: false` 다. 구조적으로 승격
  불가이고, 그게 설계 의도다.

**즉 B 전체가 "다시 파지 말 것" 이다.** 인과를 얻는 남은 길은 C1 하나다.

---

## C. 배관은 다 있고 데이터만 없는 것

### C1. 공급망 공시 수집 — 인과 술어를 얻는 유일하게 남은 정당한 길

```
정책    ✓  SUPPLIES · CUSTOMER_OF · promotionEligible true
빌더    ✓  relations/builders/supply-chain.ts — 호출자 없음
테이블  ✓  analytics.firm_supply_relation
데이터  ✗  0행
```

빌더가 `absenceSemantics: unknown_not_disclosed` 라 **공시가 없으면 아무것도
만들지 않는다** — 지어내기가 구조적으로 막혀 있다.

첫 단계는 SEC 10-K 주요 고객 공시와 DART 매출처 공시의 **수집 가능성·파싱
난이도를 재는 것**. 둘 다 아직 미확인이다.

### C2. 크립토 평면 — 4종 전부 0행

```
crypto_serving.entity_revision          0
crypto_serving.event_revision           0
crypto_serving.core_relation_revision   0
crypto_serving.risk_exposure_revision   0
```

탭이 빈 화면을 낸다. **가동하거나 탭을 숨기거나** 둘 중 하나를 골라야 한다.
Binance 파생 데이터가 이미 crypto-research 를 통해 매일 들어오므로 새 벤더가
정말 필요한지가 먼저다.

### C3. 포트폴리오 노출 사슬 — 세 단계가 전부 0

```
analytics.impact_shock              0
analytics.impact_exposure_revision  0
personalization.portfolio_snapshot  0
```

`impact_exposure_revision` 은 **의도적으로 비워 둔 것**이다 — sign·materiality·
economic_magnitude 를 지어내야 채워지고, `model_config` 로 묶어도 모델 출력이 곧
주장이 된다. 나머지 둘이 이것에 걸려 있다.

### C4. `knowledge.claim` verified 0 (corroborated 292)

문서 2건 요건을 못 채운다. **검증 기준이 이 저장소에 정의된 적이 없다.**
기준을 정하는 것이 먼저이고 그건 코드가 아니라 결정이다.

---

## D. 오늘 새로 드러난 것 — 로드맵이 틀렸던 곳

### D1. ⚠ macro_shock 97 · regulation 152 가 **이미 추출되고 있다**

`roadmap-2026-08-04.md` 6번은 "`macro_shock`·`regulation` 은 분류 체계에는 있는데
**추출된 사건이 0건**" 이라고 적었다. **지금은 아니다.**

```
policy_event  623   귀속 0
regulation    152   귀속 0
macro_shock    97   귀속 0
                    합 872건이 전부 미귀속
```

추출은 되고 **귀속이 안 된다.** 이건 로드맵이 적은 문제("들어올 자리가 비어
있다")와 다른 문제다.

**그래서 5단계(어휘 사건 → 거시 Metric 귀속)의 대상이 훨씬 크다.** 어휘에 걸린
127건이 아니라 이 872건이 같은 배관을 기다리고 있다. 거시 상관이 만든
`fred:*` 엔티티 13개와 `analytics.macro_series_topic` 이 이미 있으므로 재료는
갖춰져 있다.

**이 목록에서 값어치 대비 가장 값싼 항목으로 보인다.** 다만 "종목을 못 찾으면
주제에 붙인다" 는 판단이 필요하고 그건 사용자 몫이다.

### D2. 거시 13계열 중 10계열이 아직 측정 밖

```
fred:ICSA      주간(토요일 기준)  거래일 겹침 260일 중 0일   → 정렬 자체가 불가
fred:WALCL     주간(수요일)       겹침 258/261 (98.9%)      → 주간 리샘플 필요
월간 8종        관측 59일 중 38일  월 변화 vs 하루 수익률     → 빈도 불일치
```

`WALCL` 이 가장 값싸다 — 98.9% 가 거래일이라 **주간 리샘플한 주가 계열만 있으면
바로 들어온다.** 월간 8종은 표본 38점이라 그 다음이다.

### D3. `MACRO_COMOVEMENT` 가 화면에 안 나온다 — 의도적이지만 미완

`relation-graph-projector-v2.ts` 의 `DIRECT_PREDICATES` 에 넣지 않았다. 넣으면
`peer`(동종)로 렌더되는데 거시 계열과 종목의 상관을 "동종 관계" 라 부르는 것은
틀린 말이다.

**즉 엣지는 그래프와 임팩트 경로에 들어가지만 제품 화면에는 안 나온다.**
"함께 움직였다" 를 인과로 읽히지 않게 표시하는 별도 설계가 필요하다.

### D4. 거시 경로의 사거리가 작다 — 4경로

측정: 거시 엣지가 붙은 11종목 중 45일 창에 사건을 가진 곳은 **4곳 10건**뿐이고
(팬오션 4 · UAL 4 · DAL 1 · PLD 1), 나머지 일곱은 ETF 라 뉴스가 안 붙는다.
실제로 생긴 경로도 4개다.

넓히려면 D1(귀속) 또는 D2(계열 확대)가 먼저다. 임계값 0.25 를 0.20 으로 낮추면
엣지는 18 → 48 이 되지만 **사건이 없는 종목에 엣지를 더 붙이는 것**이라 경로는
거의 안 늘 것이다.

---

## E. 소유하고 있으나 아무도 읽지 않는 테이블

전부 마이그레이션이 심고 그 뒤 손대지 않은 것들이다.

| 테이블 | 행 | 상태 |
| --- | ---: | --- |
| `analytics.theme_membership` | 396 | **다리 놓으면 안 된다** — 전부 `adjacent` tier, 근거 396건 중 370건(93%)이 격리 |
| `analytics.theme` | 138 | 위와 한 쌍 |
| `core.security_master` | 297 | 용도 판정 안 됨 |
| `knowledge.ontology_rfc` | 22 | 용도 판정 안 됨 |
| `analytics.impact_channel` | 17 | 용도 판정 안 됨 |
| `analytics.meta_path_policy` | 4 | 용도 판정 안 됨 |

아래 넷은 **"쓸지 버릴지" 결정이 안 된 상태**로 남아 있다. 판정하는 것 자체가
일이다.

---

## F. 화면·품질

### F1. 시각 게이트 — 오늘 수정

`market-graphite` 의 `--color-text-tertiary` 가 12px 에서 AA(4.5) 미달이었다.
**기록보다 나빴다** — 핸드오프는 다크 4.48 · 라이트 4.37 로 적었는데, 그건
`surface`/`canvas` 기준이고 실제 최악은 `accent-soft` 위에서 **3.51** 이었다.

```
라이트  #6f716a → #696a64   최악 4.18 → 4.61
다크    #7d8177 → #83877d   최악 4.26 → 4.62
```

라이트에서 `--color-text-secondary` 도 `surface-muted` 위에서 4.49 로 아슬아슬하게
미달인 것을 함께 발견했다. **본문을 얹는 세 표면**(canvas/surface/surface-subtle)
기준으로는 4.84 로 통과하므로 이번에는 건드리지 않았다 — 건드리면 tertiary 와
간격이 무너진다. `surface-muted` 를 본문 배경으로 쓰기 시작하면 그때 다시 봐야 한다.

### F2. 모바일 가로 오버플로 9px — 원인 미특정

390px 에서 `scrollWidth 399`. 원인 요소를 특정하지 못했고 UI 범위라 쫓지 않았다.
다른 라우트가 대비에서 먼저 실패해 오버플로 단계까지 가지 못했으므로, F1 이
통과하면 **이 검사가 처음으로 다른 라우트에도 적용된다** — 지금 아는 것보다 넓은
문제일 수 있다.

### F3. `market_confirmation` 화면 미연결

읽기 모델은 v2 로 전환됐으나 화면에 붙지 않았다.

---

## G. 수집·커버리지 — 고칠 코드가 없는 것

### G1. 커버리지 미수집 1,907 / 3,955

```
complete            1,613
not_collected       1,907   대부분 "커서가 아직 도달하지 않음"
not_applicable        364
source_unavailable     71   OpenDART status 013 — 물었고 없음
```

**실패가 아니라 밀린 것이다.** 제약은 OpenDART 일일 한도(잡 기록으로 재면
(300, 361] 사이)와 시간이다. 발행사당 20요청은 줄일 수 없다.

### G2. `rss_news` 4,338건 중 entity_key 7건 (0.2%)

미귀속의 근본 원인. 유니버스 크기 문제가 **아니다** — 뉴스가 지목한 종목 54개가
전부 이미 유니버스 안이라는 것이 2026-08-04 에 측정됐다. 텍스트 귀속이 담당하는
영역이고 D1 과 같은 뿌리다.

### G3. 크립토 CAIP-2 7 / 14

나머지 7개(TRX·XRP·NEAR·SUI·DOGE·HYPE·ADA)는 **각 네임스페이스 규격에서 읽어야
한다 — 기억으로 적으면 안 된다.**

---

## 내가 추천하는 순서

1. **A2 다이제스트** — 배포를 막고 있고, 다른 모든 화면 작업이 여기 걸린다
2. **D1 미귀속 872건 귀속** — 재료가 이미 다 있고 사거리가 가장 크다
3. **C1 공급망 수집 가능성 조사** — 인과 술어를 얻는 유일한 길, 다만 먼저 재야 한다
4. **D2 `WALCL` 주간 리샘플** — 거시 계열 하나를 값싸게 늘린다
5. 나머지는 결정이 선행되는 것들(C2 크립토 · C4 검증 기준 · E 테이블 판정)

**1·2 가 나머지 전부보다 값어치가 크다고 본다.** 3 은 값어치는 가장 크지만
아직 잴 것이 남아 있다.
