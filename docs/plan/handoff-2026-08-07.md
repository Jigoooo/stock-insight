# 인수인계 — 2026-08-07

이 문서는 다음 세션이 **재측정 없이 이어받을 수 있는 것**과 **반드시 다시 재야 하는 것**을
가른다. 수치는 전부 이 날 실측이다.

> **정정 (같은 날 후속 세션):** §4 D · §6 · §8 이 격리 엣지를 "온톨로지 승인 한 줄에 막혀 있다"
> 로 적었다. **재측정 결과 반증됐다 — 16종을 전부 승인해도 해제되는 엣지는 0건이다.**
> 차단막은 세 겹이고 온톨로지는 그중 가운데다. 근거와 재현 질의는
> `docs/plan/quarantine-root-cause-2026-08-07.md`. 이 문서의 나머지 수치는 유효하다.
>
> **후속 세션이 §4 C 도 반증했다.** ECOS 를 "스키마 변경 없이 수집기 하나면 된다" 로
> 적었는데 수집까지만 맞았다. 그래프 연결에 막힘이 셋 있었고 전부 열었다 →
> `ecos-collector-2026-08-07.md`. 그리고 §6 부채 표는 **아홉 줄이 철회·해소됐다** —
> 대부분 고쳐서가 아니라 **적혀 있던 사유가 틀려서**다.

---

## 0-a. 후속 세션 요약 (2026-08-07 오후~저녁)

**관측으로 닫았다 — 파이프라인을 실제로 돌렸다.**

```
스냅샷            30 → 37        엣지 5,401 → 7,062 · 엔티티 672 → 691
HELD_BY           0 → 250        사상 최초
COMMON_OWNER      0 → 1,391      팩 예산으로 해제. 레거시 격리 531 이 되살아났다
MEASURED_BY      14 → 20         ECOS 5계열 + DHHNGSP
MACRO_COMOVEMENT 22 → 25         KR 은 KOSPI 팩터로 10 → 7, US 18 유지
상관 모델 계열     6 → 12
market.macro_vintage  ecos: 18,507행 신규 (KOSPI 2,846 포함)
게이트            typecheck 11/11 · oxlint 0 · 907 tests / 862 pass / 0 fail
```

### 이 세션이 틀린 방식 — §0 과 같은 모양이 계속 나왔다

| 틀린 것 | 어떻게 드러났나 |
| --- | --- |
| ownership 배선 후 "후보 1,641 · 격리 0" 을 건강함으로 읽었다 | 오류 없음. 그리고 **apply 가 `pack US:AAPL exceeds lineage item cap: 672` 로 프로덕션을 깨뜨렸다.** 팩은 관계와 **증거**를 함께 세는데 엣지 수만 봤다 |
| 그걸 고치려 넣은 팩 크기 측정이 `max 275 · 초과 0` 을 보고했다 | 같은 실행의 apply 가 672 로 죽었다. **투영이 현재 DB 기준이라 이번 실행 후보가 빠져 있었다** |
| 13F 수집기가 없어서 ownership 이 막혔다고 적었다 | **소유자 노드가 없었던 것**이다. 필요한 건 `LegalEntity` 13개 |
| 팩 상한 512 를 "마이그레이션 026 이 강제한다" 고 적었다 | **026 에 512 가 0회 등장한다.** 순수 앱 상수다. DB 는 잘림을 이미 허용한다 |
| "ownership 엣지가 팩을 밀었다" | **HELD_BY 는 팩에 0 기여.** 투영이 안 받는다. 전적으로 COMMON_OWNER |
| `^KS11` 이 "141일치" | **141은 행 수, 날짜는 57일**, 2026-07-24 정체. 팩터 창은 1,095일 |
| 13F 신선화를 "홀딩스 갱신" 으로 규정 | sibling 스크립트가 **`graph_edges` 2,542행을 지우고 재생성**한다. 범위가 다르다 |

**교훈 둘이 붙는다.**
① 드라이런이 건강한 것도 옳음의 증거가 아니다 — **apply 와 같은 것을 재고 있는지** 봐라.
② **강제되는 예산은 예측할 필요가 없다.** 추정기를 고치려 애쓰다 실측 보고로 바꿨다.

### 남의 장애도 하나 고쳤다

knowledge 파이프라인이 **08-07 01:46부터 죽어 있었다**(이 세션 시작 02:09 이전).
`claim ... could not bind current-revision chunk evidence` — 검증은 인용문 **앞 40자**,
바인딩은 **전문**을 요구했다. 두 기준을 한 함수로 묶었다.
증거는 `knowledge.claim` 이 348 에서 멈췄는데 오류는 349·350 을 부른 것 — 시퀀스만
전진하고 행은 롤백됐다.

### 가장 값있는 두 관측

**하나 — 격리 탈출 경로가 실증됐다.** 세션 초반에 "온톨로지 승인이 아니라 증거를 가진
새 accepted 개정이 유일한 출구" 라고 진단했는데, COMMON_OWNER 를 켜자 레거시 격리
1,062 중 **531 이 정확히 그 방식으로 되살아났다.**

**둘 — 시장 팩터 교체는 맞바꾸기가 아니라 결함 수정이었다.** 합성지수는 통제 대상
종목들의 평균이라 각 종목이 자기 자신에서 일부 빠진다. append-only 원장이 옛 개정을
보존해 직접 쟀더니 종목-시장 상관이 **6/7 하락·상승 0**. 잃은 3 엣지는 부풀려서
만들어진 것이다.

---

## 0. 먼저 읽을 것 — 이 세션이 틀린 방식

세 번 틀렸고 **세 번 다 삽입 수가 멀쩡해 보였다.**

| 틀린 것 | 어떻게 드러났나 |
| --- | --- |
| 의회 수집기가 1995~2016 액션을 넣었다 | 오류 없음. 499행 삽입. **날짜 범위를 보고서야** |
| `URLSearchParams` 가 `+` 를 `%2B` 로 인코딩 → API 가 정렬을 조용히 무시 | 오류 없음. 494행 삽입. 2025-01~04 만 |
| `EXPOSES` 를 "없다, 만들어야 한다" 로 판정 | **107개가 이미 원장에 있었다.** 재보고 정정 발행 |

**교훈은 하나다: 건강한 카운트는 옳음의 증거가 아니다.** 삽입 수 말고
**범위·모양·상태**를 봐라.

---

## 1. 지킬 작업 규율 (사용자 지시, 계속 유효)

- 관측으로 닫는다. "고쳤다" 가 아니라 **재측정한 수치**로 완료를 부른다
- 지어내지 않는다. 근거가 없으면 만들지 말고 **왜 안 만들었는지 기록**한다
- 못 한 것도 근거와 함께 보고한다. **조용히 범위를 줄이지 않는다**
- 커밋 메시지는 **한국어**로, 무엇을 **왜 하지 않았는지**까지
- 모든 응답은 한국어
- **자격증명 값은 절대 출력하지 않는다.** 이름만 보고하고 값은 셸 변수로 넘긴다

> 원래 "프로덕션 DB 쓰기와 배포는 실행 전 확인" 규칙이 있었으나 이 세션에서
> **"나한테 물어보지말고 계속 진행해"** 로 명시적으로 해제됐다. 새 세션에서는
> 기본값(확인받기)으로 돌아간다고 보는 게 안전하다.

---

## 2. 환경

```
작업 경로   /home/jigoo/.hermes/workspace/stock-insight
DB          postgresql://research_app@127.0.0.1:55432/research_app
env         /home/jigoo/.hermes/.env        (node --env-file 로 넘긴다)
게이트      pnpm typecheck · pnpm lint(oxlint) · pnpm test
마지막 상태 typecheck 11/11 · oxlint 0 · 894 tests / 848 pass / 0 fail
세션 커밋  0aae2af..HEAD (master, push 완료) — 시작 전 `git log --oneline -8` 로 확인
```

### 치명적 함정 — 반드시 알고 시작할 것

1. **파이프라인 타이머가 워킹 트리를 실행한다.** 파일을 **편집하는 순간 배포**다.
   "커밋 전이라 안전하다" 는 여기서 거짓이다. 이 세션에서 마이그레이션 적용 전
   링커를 워킹 트리에 둬서 **파이프라인을 세 번 깨뜨렸다**
2. `pipeline_resolve_provenance` 가 시작·종료 시점에 git 추적 파일을 해시한다.
   실행 중 편집하면 `failed` 로 기록된다
3. `research_app` DB 는 **네 프로젝트 공유**다. 표를 건드리기 전
   `docs/operations/database-ownership.md` 를 열어라 — 그 문서에 함정 네 개가
   적혀 있고 이 세션은 **읽기 전에 그중 셋을 밟았다**
4. `source-contract-integrity.test.ts` 는 `STOCK_INSIGHT_SOURCE_REVISION_TEST_DB_URL`
   없으면 **조용히 skip** 한다. 마이그레이션 069 의 계약 위반이 하루 숨었던 이유

---

## 3. 이번 세션에 한 것 (재측정 불필요)

### A — 거시 계열 추가 · `2897f92`

```
fred:DHHNGSP  헨리허브 천연가스   680 관측
energy 주제 계열  1 → 2          (WTI 단독이던 것)
```

**금·은·구리는 재보고 뺐다.** 계획이 "FRED 가 이미 제공" 이라 적었으나 확인 안 한
가정이었다 — FRED 는 금·은 **현물가를 게시하지 않고**(LBMA 고시 중단), 구리는
**월간**이라 상관 모델이 버린다.

`macro-series-list-parity.test.ts` 추가 — 수집기 배열과
`analytics.macro_series_topic` 이 어긋나는 것을 양방향으로 막는다. 면제 목록은
**측정 결과 비어 있다**.

### B — 버려지던 일정 승격 · `ee35b44` + `93a327a`

**가장 큰 발견: 일정은 없는 게 아니라 버려지고 있었다.** `research-common` 이
`event_calendar.py` · `macro_calendar.py` 를 **매일 스케줄로** 돌려 JSON 을 쓰는데,
Postgres 로 가는 경로가 파생 시그널 카드를 남기고 **날짜를 버린다.**
새 수집이 아니라 **한 번도 안 만든 마지막 홉**이었다.

```
market.scheduled_event  (마이그레이션 074 · 075)
  legislative_action    495   2025-01-09 ~ 2026-08-05 · 하원 168 · 상원 327
  economic_release      173   actual·consensus 포함
  earnings               50
  central_bank_meeting    1   2026-08-28 BOK 금통위 ← 유일한 진짜 미래 일정
```

- `run-calendar-promote.ts` — `policy_events` 는 **거부**한다. 사유명
  `policy_events_are_headlines_not_schedules` (헤드라인이지 일정이 아님)
- `run-congress-bills.ts` — `--pages 2` 로 `run_market_enrichment.sh:120` 에 배선.
  위 두 함정이 주석과 테스트에 박혀 있다

---

## 4. 안 한 것 — 그리고 **왜 안 했는지가 세션 중에 바뀌었다**

처음에 "용량 때문" 이라고 적었는데 **재보지 않고 쓴 변명**이었다. 재니 D·E 는
막힌 게 아니라 **잘못 짚은 것**이었다.

### C — 한국 거시 (ECOS) · 사용자가 다음 세션으로 미룸

준비 상태는 확인했다.

```
ECOS_API_KEY          .env 에 있고 라이브 확인함 (StatisticTableList → 통계표 834개)
ECOS_SERIES           이미 identifier_type CHECK 에 있다 (008_core_ingestion_foundation.ts:32)
market.macro_vintage  그대로 재사용 가능
```

**스키마 변경 없이 수집기 하나면 된다.** `run-fred-vintage.ts` 가 그대로 본보기다.
`macro_series_topic` 도 함께 넣어야 한다 — A 에서 만든 parity 테스트가 안 그러면 잡는다.

### D — EXPOSES · **오진이었다. 크기가 다르다**

```
EXPOSES 원장 엣지    107
상태                 quarantined_unverified
막은 것              policy_status = 'provisional_review_required'
서술어 설명란        "semantic approval pending"
그 107개의 모양      Stock → Industry   ← 정책→기업이 아니다
```

**그리고 근본은 따로 있다 — 법안이 엔티티가 아니다.**

```
core.entity 아홉 종류
  Metric 329 · Company 325 · Stock 325 · LegalEntity 156 · Theme 138
  Industry 112 · Token 24 · ETF 18 · Exchange 3
  ← 법안도 일정도 없다
```

495개 법안 액션은 `market.scheduled_event` 라는 **평범한 표**에 있지 그래프 노드가
아니다. **`EXPOSES` 를 승인해도 이을 쪽 끝이 없다.**

> **D 는 "잡 하나 쓰기" 가 아니라 "법안을 엔티티로 볼 것인가" 라는 설계 결정이다.**
> 사용자 확인이 필요한 종류의 일이다.

**그리고 이건 EXPOSES 만의 문제가 아니다 — 이 세션 최대 미청구 발견:**

```
격리된 엣지  3,312   서술어 16종
  COMMON_OWNER 1,062 · AFFECTS 616 · PEER_OF 174 · ROLLS_UP 129 · EXPOSES 107 ...
```

계산이 끝나 원장에 들어와 있는데 **온톨로지 승인 한 줄**에 막혀 서빙까지 못 간다.
"만들어 놓고 안 쓴다" 의 **가장 큰 사례**인데 이 세션은 마지막에야 셌다.

### E — 유니버스·ETF · **등급을 잘못 매겼다 (3등급 → 2등급)**

계획이 "남의 저장소" 로 분류했는데 **`public.entities` 를 보고 거기서 멈춘 것**이었다.

```
core.v_security_universe = core.entity + core.listing + core.entity_identifier
                           ← 전부 우리 스키마. public.entities 를 안 읽는다
run-core-identity-sync.ts  ← 우리 잡. core.entity 에 쓴다
  :27  FROM public.entities legacy   ← 남의 것은 이 *읽기* 하나뿐
KRX_API_KEY                .env 에 있음. 읽는 코드 0개
```

**KRX 피드를 그 잡의 두 번째 원천으로 붙이면 교차 저장소 쓰기가 아니다.**

다만 **넓히는 게 목적이 되면 안 된다.** 이 세션이 잰 것:

```
미귀속 사건 2,641 중 유니버스 회사를 언급한 것   163 (6.2%)
```

나머지 93.8% 가 미귀속인 것은 **필터가 맞게 도는 것**이지 결함이 아니었다.
KOSPI/KOSDAQ 전체는 194 → 2,500+, **13배**고 비용도 13배다.
**"무엇을 보고 싶은가" 를 먼저 정해야 한다.**

---

## 5. 열쇠 아홉 개가 안 읽히고 있다

```
CONGRESS_GOV(이번에 씀) · ECOS · BOK_ECOS · KOSIS · KRX · DATA_GO_KR
ALPHA_VANTAGE · FMP · FINNHUB        → 참조 파일 0
```

"선언은 있고 부르는 코드가 없다" 가 **자격증명 층위**에서 반복된 것이다.
확장을 계획할 때 **새 원천을 찾기 전에 이 목록부터** 봐라.

---

## 6. 열려 있는 부채 (기록만 됨, 미착수)

| 항목 | 크기 | 근거 |
| --- | --- | --- |
| ~~격리 엣지 3,312 / 서술어 16종~~ **철회** | ~~최대~~ **0** | 승인으로 해제되는 엣지 0건. 설계대로 작동하는 상태이지 부채가 아니다 → `quarantine-root-cause-2026-08-07.md` |
| ~~`ownership` 빌더가 테스트 전용 (13F 수집기 부재)~~ **해소** | ~~중~~ **0** | 막은 것은 수집기가 아니라 **소유자 노드**였다. 피보유 측은 250/250 이미 해소돼 있었고 `core.entity` 에 기관이 0개였다. 마이그레이션 077 로 `LegalEntity` 13개를 만들고 배선 → **`HELD_BY` 250 accepted** |
| ~~`fred:DHHNGSP` 680 빈티지 · 그래프 관계 0건~~ **부분 해소** | 소 | `TRANSFORMS`·`FREQUENCY` 에 넣어 이제 로드되고 `MEASURED_BY` 도 받는다. 다만 **상관 엣지는 여전히 0** — 325종목 어느 것과도 임계를 못 넘었다. 측정 결과이지 결함이 아니다 |
| ~~ECOS 5계열이 수집만 되고 그래프 미연결~~ **해소** | ~~중~~ **0** | 조인을 `ECOS_SERIES` 까지 넓히고 마이그레이션 076 적용 → **상관 엣지 6개**(국고채3년 2·10년 2·회사채AA- 2). CD·기준금리는 0이고 계단함수라 예측대로다 |
| ~~`market_ts.ohlcv` 소유권 문서 모순~~ **해소** | ~~중~~ **0** | 양쪽이 쓴다(ict-bot 236만 · yfinance 30만, 키공간 교집합 0). 문서를 실측으로 고치고, 교집합 0 불변조건을 `run_ohlcv_daily.sh` DB 단언에 넣었다(1.9초) |
| ~~`analytics.theme` 534행 폐기 예정~~ **분류 철회** | 중 | 폐기 근거였던 `theme_exposure_snapshot` 이 **존재하지 않는다**(계획서에 축만 있음). 534 = theme 138 + membership 396 → `debt-remeasured-2026-08-07.md` §2 |
| ~~SEC `legacy_no_document` 1,478 재수집~~ **철회** | ~~중~~ **0** | 재수집으로 귀속되는 건수 **0**. 855건 전부 유니버스 밖 기업이다. 같은 문서 §1 |
| ~~`source-contract-integrity.test.ts` 기본 skip~~ **해소** | ~~소~~ **0** | 커버리지 단언을 `run-source-contract-audit.ts` 로 옮겨 분석 파이프라인이 매일 돌린다. 위반 시 런이 깨진다 |
| ~~`COMMON_OWNER` 1,391 이 팩 예산에 막힘~~ **해소** | ~~중~~ **0** | 투영에 술어별 라운드로빈 예산을 넣어 풀었다. accepted **1,391**, 팩 max **460**/상한 512, overCap **0**. 부수로 **레거시 격리 531 이 되살아났다** |
| 13F 신선화 | 소 | **CUSIP 이 아니다 — 신선도 문제다.** 다만 수확이 거의 0: 대형 4곳이 42종목 중 39종목을 모두 보유해 **42는 이름 매칭 상한**이고, 재수집해도 새 `HELD_BY` 쌍은 안 생긴다. 비용은 sibling 의 `graph_edges` COMMON_OWNER **2,542행 삭제 후 재생성**. 사용자 판단으로 **안 돌린다** |
| `HELD_BY` 경로 스텝 3,124 → 40 | 소 | `COMMON_OWNER`(1,292)에 밀렸다. 사건당 술어 분포는 나빠지지 않아(4종 162→175) 켜 둔다. 다만 이 원천의 공동보유는 인덱스 펀드라 설명으로서 약하다. 되돌리려면 `SHIPPED_OWNERSHIP_PREDICATES` 한 줄 |
| ~~KOSPI 로 한국 시장 팩터 교체~~ **완료** | ~~중~~ **0** | KR accepted 10 → 7, US 18 → 18. 잃은 3은 합성지수가 부풀린 것 — 종목-시장 상관이 6/7 하락·상승 0. **맞바꾸기가 아니라 측정 결함 수정** → `ecos-collector-2026-08-07.md` §10 |
| `policy_event` 611 미귀속 | 중 | 576건이 시장 어휘에 하나도 안 걸린다. 살릴 유일한 경로는 **사건→테마 분류기**이고 `analytics.theme` 534행이 그 재료다 → `debt-remeasured-2026-08-07.md` |
| `public.entities` **21개** 파일 참조 | 소 | `007_serving_read_layer.ts:11` 이 "transitional source … until core schema lands". **계획서의 "12개" 는 틀렸다 — 실측 21** |

---

## 7. 보고서(아티팩트) 취급 규칙

```
URL      https://claude.ai/code/artifact/779166a2-f930-4c4b-bd4d-57a461d2b1e5
소스     docs/operations/data-ecosystem-artifact.html
favicon  🔗 로 유지 (사용자가 탭 아이콘으로 문서를 찾는다)
```

- **갱신은 반드시 `url` 파라미터로.** 빼면 새 URL 이 생기고 사용자가 보던 링크는
  옛 내용에 멈춘다
- **「생태계 감사」(`eaa04f70-12b5-4b35-942b-8eb0b90d2f9b`) 는 2026-08-02 자 기록.
  손대지 말 것**
- 발행 전 태그 균형 검사를 돌린다(이 세션은 매번 했다)

---

## 8. 다음 세션 권장 순서

```
1  C — ECOS 수집기          가장 싸고 막힘이 없다. 스키마 준비됨, 키 확인됨
2  격리 엣지 3,312 조사      최대 미청구. "왜 16종이 미승인인가" 를 먼저 재라
3  D — 법안 엔티티화 결정     설계 판단. 사용자 확인 필요
4  E — KRX 원천 추가         2등급으로 내려왔으나 "무엇을 볼 것인가" 가 선행
```

~~**2번을 1번보다 먼저 둘 수도 있다**~~ — **재봤고, 유보는 해소됐다.** 3,312 쪽 회수
가능량은 0이다. **순서는 C 가 먼저**이고 2번은 사용자 설계 판단 사항으로 내려갔다.
갱신된 순서는 `quarantine-root-cause-2026-08-07.md` §6.
