# 인수인계 — 2026-08-05

이 문서를 먼저 읽고 시작하면 된다. 함께 읽을 것:

- `docs/operations/database-ownership.md` — **가장 먼저.** 이 DB 는 네 프로젝트가 공유한다
- `docs/plan/handoff-2026-08-04.md` — 어제까지의 맥락과 오류 5가지
- `docs/plan/handoff-2026-08-03.md` — 그 전날의 함정 7가지
- `docs/plan/exposure-predicate-design-2026-08-04.md` — 측정으로 죽은 가설 6개.
  EXPOSES · ownership 배선 · 유니버스 확장은 다시 파지 말 것
- `docs/plan/roadmap-2026-08-04.md` 2b — 거시 상관 설계

## 이 작업의 규율

- **관측으로 닫는다.** "고쳤다" 가 아니라 재측정한 수치로 완료를 부른다
- **지어내지 않는다.** 근거가 없으면 만들지 않고, 왜 안 만들었는지 기록한다
- **못 한 것도 근거와 함께 남긴다.** 조용히 범위를 줄이지 않는다
- 커밋 메시지는 한국어로, 무엇을 왜 하지 않았는지까지
- 프로덕션 DB 쓰기와 배포는 실행 전에 확인받는다

## 이번 세션에 한 것 — 거시 상관 2·3·4단계

술어 이름은 **`MACRO_COMOVEMENT`**. 인과로 읽히지 않아야 한다는 요건 때문에
`AFFECTS`·`EXPOSES` 를 쓰지 않았다. 잰 것은 "함께 움직였다" 하나뿐이다.

| 단계 | 파일 | 상태 |
| --- | --- | --- |
| 2 정책 | `relations/relation-policy.ts` | 완료 |
| 2 온톨로지 | 마이그레이션 `066_macro_comovement_ontology` | **코드만. 프로덕션 미적용** |
| 3 모델 | `relations/macro-comovement-model.ts` | 완료 |
| 3 빌더 | `relations/builders/macro-comovement.ts` | 완료 |
| 4 배선 | `analytics/run-v2-graph-publish.ts` | 완료. **apply 미실행** |

### 드라이런 관측 (2026-08-04 15:00 UTC, 라이브 DB 읽기 전용)

```json
{"seriesLoaded":3,"stocksLoaded":325,"pairsWithEnoughOverlap":759,
 "pairsOverThreshold":18,"pairsDroppedByDegreeCap":0,
 "candidates":18,"accepted":18,"quarantined":0}
```

같은 수치를 **독립적인 SQL 로 먼저 재고 코드가 그 값을 재현했다.** 두 경로가
공유하는 것이 없으므로(한쪽은 psql, 한쪽은 TS 모델) 18이라는 값은 교차 검증됐다.

18쌍 전부 경제적으로 말이 된다 — 이게 배관이 아니라 측정이 돌고 있다는 증거다.

| 계열 | 종목 | r | 읽는 법 |
| --- | --- | ---: | --- |
| DGS10 | TLT (20년+ 국채 ETF) | **-0.902** | 장기채 가격은 금리와 반대. 교과서 |
| DGS2 | TLT | -0.605 | 만기가 짧아 덜 반응 |
| DGS2 | UUP (달러 ETF) | +0.450 | 단기금리와 달러 |
| DGS10 | HD 홈디포 | -0.431 | 주택 민감 소매 |
| DEXKOUS | UUP | +0.347 | 둘 다 달러 강세 |
| DGS10 | XOM · XLE | +0.344 · +0.340 | 에너지와 금리 |
| DEXKOUS | 팬오션 | +0.324 | 달러로 버는 해운 |
| DGS2 | GLD | -0.269 | 금과 실질금리 |

### 13계열 중 3계열만 잰다 — 나머지 열을 왜 뺐는가

**설계에 앞서 정렬을 먼저 쟀다.** `planPriceCorrelations` 계열의 정렬은 날짜를
정확히 교집합한다. 계열별로 관측일이 미국 거래일과 겹치는 비율(2021-08 이후):

```
fred:DGS10 · DGS2       1,256 / 1,305   96.2%   일간   → 쓴다
fred:DEXKOUS              648 /   675   96.0%   일간   → 쓴다
fred:WALCL                258 /   261   98.9%   주간   → 뺀다
fred:ICSA                   0 /   260    0.0%   주간   → 뺀다
월간 8종                   38 /    59   64.4%   월간   → 뺀다
```

- **`ICSA` 는 0이다.** 주간 집계 기준일이 토요일이라 거래일과 절대 안 겹친다.
  넣었으면 조용히 0쌍을 내고 "빌더가 도는데 결과가 없다" 로 보였을 것이다
- **`WALCL` 은 98.9%가 겹치지만 주간이다.** 주 대비 변화를 하루치 주가 수익률과
  맞대는 것은 빈도 불일치다. 주간으로 리샘플한 주가 계열이 있어야 잴 수 있다
- **월간 8종도 같은 이유** 에 표본이 38점뿐이다

계열별 사유는 `MACRO_SERIES_EXCLUSIONS` 에 문자열로 박아 뒀다. 문서에만 적으면
코드를 고치는 사람이 못 본다.

### 수익률 정의를 계열별로 갈랐다

**금리에 수익률 공식을 쓰면 안 된다.** `DGS2` 가 0.10에서 0.20으로 가면 퍼센트
변화로는 +100%가 된다. 금리는 **레벨 차분**(퍼센트포인트), 환율과 주가는
**로그 수익률**. 둘 다 그 자료형의 표준이고, 둘 다 `model_config` 에 이름으로 들어간다.

테스트가 이걸 지킨다: 로그 수익률이 금리 차분의 정확히 -2배인 종목을 만들면
상관이 -1이 나와야 한다. 금리를 수익률로 돌리면 이 등식이 깨진다.

### model_config 에 묶은 것

창 길이 365 · 임계값 0.25 · 표본 최소 60 · 계열별 변환 · 주가 변환 ·
계열 id · vintage 선택 규칙 · 정렬 규칙 · 계열당 차수 상한 25 · 코퍼스 다이제스트.
handoff-2026-08-04 가 요구한 항목을 전부 포함한다.

**임계값 0.25는 재서 골랐다.** 0.20이면 48쌍, 0.25면 18쌍, 0.30이면 11쌍.
18쌍을 눈으로 읽고 전부 말이 되는 것을 확인한 뒤 고정했다. 바꾸려면 상수 하나이고
효과는 위 숫자로 이미 알려져 있다.

### 증거를 두 창으로 잡은 이유

`minSourceRevisions: 2` 다. 상관은 **거시 계열 창과 주가 창 둘이 있어야** 생기고,
하나만으로는 재현이 안 된다.

`model_config` 만으로는 안 된다는 것이 중요하다. 마이그레이션 024의
`guard_accepted_relation_revision` 은 적격 증거로 `identity_mapping`·`claim`·
`document`/`chunk`·`source_revision` 만 인정한다 — **`model_config` 는 목록에 없다.**
그것만 달고 accepted 를 넣으면 격리가 아니라 **예외가 나서 퍼블리시 트랜잭션 전체가
롤백된다.** 그래서 빌더는 반드시 `source_revision` 증거를 함께 낸다.

두 창의 리비전은 이 잡이 직접 만든다. `market.macro_vintage` 는 `source_revision_id`
가 일부 행에만 있고(DGS10 은 22,059 중 10,691), `market_ts.ohlcv` 는 계보 컬럼 자체가
없다(research-common 소유, 우리는 읽기만). 있는 것만 골라 인용하면 실제로 쓴 입력을
다른 것으로 적는 셈이라, ETF·프로파일과 같은 방식으로 **쓴 창을 그대로 등록**한다.

**후보를 낸 끝점에만 raw object 를 만든다.** 325종목 전부 매일 찍으면 아무 관계도
인용하지 않는 주가 창을 연 1GB 가까이 쓰게 된다. 후보가 의존하는 13개 안팎만 남긴다.
탈락한 쌍은 raw object 가 아니라 요약의 카운트로 남는다.

### 화면에는 안 나온다 — 일부러

`relation-graph-projector-v2.ts` 의 `DIRECT_PREDICATES` 에 `MACRO_COMOVEMENT` 를
**넣지 않았다.** 넣으면 `peer`(동종) 로 렌더되는데, 거시 계열과 종목의 상관을
"동종 관계" 로 부르는 것은 틀린 말이다. 게다가 그 경로는 양 끝이 다 종목일 것을
요구하므로 Metric 엔티티는 어차피 걸러진다.

즉 이 엣지는 **그래프와 임팩트 경로에는 들어가고 제품 관계 그래프에는 안 나온다.**
화면에 내보내려면 "함께 움직였다" 를 인과로 읽히지 않게 표시하는 별도 설계가 필요하다.

## 닫는 관측 — 셋 중 하나는 이 단계로 못 닫는다

handoff-2026-08-04 가 세 가지를 걸었다. 정직하게 가른다.

| # | 관측 | 상태 |
| --- | --- | --- |
| 1 | 13개 계열 엔티티가 스냅샷 엣지를 갖는다 (지금 0) | **apply 후 확인 가능.** 드라이런이 18엣지를 예고 |
| 2 | 미귀속 사건이 만든 임팩트 경로가 0을 벗어난다 | **이 단계로는 못 닫는다. 아래 참조** |
| 3 | 경로가 밟는 술어 목록에 새 술어가 등장한다 | **apply + 애널리틱스 퍼블리시 후 확인** |

### 2번이 왜 안 움직이는가 — 근거

두 가지가 각각 막는다.

1. **`loadRecentEvents` 가 `target_entity_id IS NOT NULL` 로 거른다**
   (`run-v2-analytics-publish.ts`). 미귀속 사건은 엣지가 아무리 많아져도 걷기를
   시작하지 못한다. 엣지가 없어서가 아니라 출발점이 없어서다.
2. **`policy_event` 623건이 붙은 Metric 엔티티는 FRED 13계열이 아니다.**
   `gl_major_event`·`crypto_regulation`·`tariffs`·`narrative_*` 이고, 이번에 엣지를
   얻은 것은 `fred:DGS10`·`DGS2`·`DEXKOUS` 다. 서로 다른 노드다.

**5단계가 따로 필요하다** — 시장 어휘에 걸린 사건(34개 용어 · 88매칭)을 해당
`topic` 의 거시 Metric 엔티티에 귀속시키는 것. `analytics.macro_series_topic` 이
이미 계열↔topic 을 잇고 있으니 재료는 있다. 다만 그건 귀속 규칙을 바꾸는 일이고
이번 범위가 아니었다. **범위를 줄인 것이 아니라 처음부터 다른 작업이었고, 이걸
2·3·4의 성공 조건으로 걸어 둔 것이 handoff-2026-08-04 의 오판이다.**

### 3번이 왜 움직일 것으로 보는가

`impact-path-builder.ts` 에서 Stock 이 아닌 노드는 종단이 아니다
(`stockEntityIds.has(edge.objectEntityId)` 일 때만 경로를 닫고, 아니면 계속 걷는다).
그리고 사건의 출발 종목은 종단 집합에서 지역적으로 제거된다. 따라서
**종목A → 거시계열 → 종목B** 가 성립한다. 이건 `CLASSIFIED_AS` 두 번이 아닌
첫 2홉 모양이다. **예측이지 관측이 아니다** — apply 전에는 확인할 수 없다.

## 지금 바로 이어서 할 것

### 1. 마이그레이션 066 적용 + apply 실행 — 사용자 확인 대기

둘 다 프로덕션 쓰기다. 순서와 확인 방법:

```bash
# 1) 066 적용 (온톨로지 행 1개)
# 2) 애널리틱스 파이프라인 또는 아래를 직접
cd apps/api && DATABASE_URL=… node src/analytics/run-v2-graph-publish.ts --apply
# 3) 애널리틱스 퍼블리시까지 돌아야 3번 관측을 볼 수 있다
```

**066 은 리더 권한을 건드리지 않는다** — `knowledge.predicate_ontology_revision` 에
INSERT 하나뿐이고 GRANT·CREATE 가 없다. 065 와 달리 다이제스트가 움직일 이유가 없지만,
`repin-live-database-digests.mjs` 는 배포 전에 어차피 돌려야 한다.

적용 전 읽기 전용으로 이미 검증했다: 삽입될 행은 `revision_no=1` 하나이고
`association`·`approved` 둘 다 체크 제약을 통과한다.

### 2. apply 후 재측정 — 이 세 쿼리로 닫는다

```sql
-- 관측 1: Metric 엔티티를 지나는 스냅샷 엣지 (지금 0)
SELECT count(*) FROM analytics.graph_snapshot_edge edge
 JOIN core.entity e ON e.entity_id IN (edge.subject_entity_id, edge.object_entity_id)
 WHERE e.entity_type='Metric';

-- 관측 3: 경로가 실제로 밟는 술어
SELECT edge.predicate, count(*)
  FROM analytics.impact_path_v2 path
  CROSS JOIN LATERAL jsonb_array_elements_text(path.explanation->'predicates') AS step(id)
  JOIN analytics.graph_snapshot_edge edge ON edge.graph_snapshot_edge_id = step.id::bigint
 GROUP BY 1 ORDER BY 2 DESC;

-- 술어별 스냅샷 엣지 (기준선: PRODUCT_SIMILARITY 38708 · SAME_ETF_BASKET 38249
--                     · ISSUED_BY 4318 · CLASSIFIED_AS 2023 · MACRO_COMOVEMENT 0)
SELECT predicate, count(*) FROM analytics.graph_snapshot_edge GROUP BY 1 ORDER BY 2 DESC;
```

**1·3이 함께 움직여야 산 것이다.** 1만 움직이면 엣지는 생겼는데 경로가 안 쓰는 것이고,
그때는 `MAX_HOPS`·`HOP_DECAY` 나 사건 랭킹의 `connectedEntityIds` 필터를 봐야 한다.

### 3. 그 다음 후보

- **5단계 — 시장 어휘 사건을 거시 Metric 에 귀속.** 위 2번 관측을 여는 유일한 길
- **월간·주간 계열을 살리려면 리샘플링.** `WALCL` 은 98.9%가 거래일이라 주간
  리샘플한 주가 계열만 있으면 바로 들어온다. 월간 8종은 표본 38점이라 그 다음
- **7번 공급망 공시 수집.** 인과 술어를 얻는 유일하게 남은 정당한 길.
  `SUPPLIES`·`CUSTOMER_OF` 는 정책·빌더·테이블이 다 있고 데이터만 0행이다

## 이번 세션에 낸 오류 — 같은 순서로 틀리지 않도록

앞선 두 문서의 7가지 + 5가지에 더해:

1. **정렬을 재기 전에 설계할 뻔했다.** 13계열 전부를 대상으로 빌더를 만들려 했는데,
   관측일과 거래일의 겹침을 먼저 재 보니 10계열이 못 쓰는 것이었다. 그대로 갔으면
   빌더는 "정상 동작" 하면서 3계열분만 내고 나머지는 `minOverlappingObservations`
   미달로 조용히 사라졌을 것이다. **조용한 스킵은 오류로 안 보인다**
2. **`adj_close` 를 쓸 뻔했다.** 이름이 맞아 보이지만 주식 1D 298,754행 **전부
   NULL** 이고 `adjustment_version` 도 비어 있다. 컬럼이 있다는 것이 값이 있다는
   뜻이 아니다 — handoff-2026-08-03 오류 6번과 같은 모양
3. **`model_config` 가 증거로 충분하다고 읽을 뻔했다.** 정책의
   `requiresModelConfig` 는 통과시키지만 DB 가드는 인정하지 않는다. 정책 통과와
   가드 통과가 다른 관문이다
4. **superhub 상한이 자르는 게 아니라 버린다.** `evaluateRelationCandidate` 는
   상한 초과 시 트리밍이 아니라 `rejected` 다. 모델 상한을 정책 상한 아래로 두지
   않으면 전량 폐기된다. 테스트로 고정해 뒀다

## 배포 상태

프로덕션 `e989889`. **그 이후 커밋은 전부 미배포.** DB 는 065 까지 반영돼 있고
066 은 아직 아니다 — 즉 지금은 **DB 가 앞서고 앱 이미지가 뒤에 있는데, 066 을
적용하면 DB 가 한 칸 더 앞선다.** 이번 변경은 수집·분석 잡 쪽이라 화면 영향은 없다
(위 "화면에는 안 나온다" 참조).

배포 전 반드시:

```bash
bash ops/scripts/verify-production-compose.sh
DATABASE_URL=… node ops/scripts/repin-live-database-digests.mjs
bash ops/scripts/verify-table-ownership.sh
bash ops/scripts/verify-systemd-units.sh
```

`verify-table-ownership.sh` 를 특히 볼 것. 이번에 `market_ts.ohlcv` 를 **읽는**
코드가 늘었다. 쓰지 않으므로 통과해야 정상이고, 실패하면 내가 어딘가에 썼다는 뜻이다.

`pnpm verify:release` 는 여전히 끝까지 통과하지 못한다. `test:workspace:visual:production`
이 이 세션 이전부터 실패한다 — 10개 라우트 전부 Axe color-contrast 이고, 원인은
디자인 프로파일 `market-graphite` 의 `--color-text-tertiary` 가 12px 에서 4.48
(AA 요구 4.5)인 것이다. 토큰 소유자의 결정이라 손대지 않았다.

## 이번 세션에 통과한 게이트

```
pnpm typecheck   11/11
pnpm build        7/7
pnpm test        10/10   (신규 macro-comovement.test.ts 11건 포함)
oxlint           apps/api 오류 0
oxfmt            통과
드라이런          18 accepted / 0 quarantined
066 SELECT 검증   읽기 전용으로 삽입 예정 행 1개 확인
```
