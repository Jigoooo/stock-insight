# Task 4 implementation report

- Added `e2e/history-preview-experience.spec.ts` with 14 behavior-focused contracts spanning desktop and mobile: fixed order, exact selection and opener focus, drawer/modal/bottom-sheet geometry, overlay and Escape close-only behavior, no-request presentation switching, independent width memory, 1240px stacking, 420px and modal wrapping, all six scenarios, localized partial failure, successful detail retry, HTTPS-only links, forbidden advisory/verdict copy, dark mode, reduced motion, Axe, and overflow.
- Fixed the isolated Playwright harness to forward `VITE_ENABLE_DEV_PREVIEW=1` only to the development Vite child. The production child still strips both development surface flags. The server-launcher contract covers both boundaries.
- Fixed one product defect exposed by the browser RED: selected History metadata in dark mode measured 3.98:1 contrast at 11px. The selected surface now uses the secondary text token for that metadata; the final Axe run is clean.
- RED evidence: launcher Node 8 passed / 1 failed because `VITE_ENABLE_DEV_PREVIEW` was missing; the first History desktop run failed 14/14 at the preview route. After the harness fix, 10 passed / 4 failed: one real dark-contrast defect and three test-contract issues (desktop two-column order was geometric rather than DOM order, dialog inerting hid role locators, and the overlay target was not settled/free of the drawer). The test issues were narrowed without weakening behavior.
- GREEN evidence: History desktop/mobile 22 passed / 6 viewport-conditional skipped / 0 failed on isolated port 16148. Today, Stocks, and Market Connections desktop/mobile fresh regression: 67 passed / 23 viewport-conditional skipped / 0 failed on port 16149. A one-off parallel Stocks `boundingBox()` null was not reproducible in three serial repeats and the fresh full rerun passed.
- Focused Node: `scripts/e2e-server-launcher.test.mjs` 9/9; History model, structure, inspector, shared frame, and dev routing 37/37.
- Full gates: format 1,356 files; lint 0 errors with six existing warnings; typecheck 11/11 tasks; test 10/10 tasks including web 733/733; build 7/7 tasks.
- `pnpm verify:release` passed lint, typecheck, P6 fixture typecheck, full test, and hard design 17/17. Its first environment-gated failure was `test:p6:db`: missing `P6_REHEARSAL_ADMIN_DATABASE_URL` produced `ERR_INVALID_URL` with `input: ''`. `XG_REHEARSAL_ADMIN_DATABASE_URL`, `STOCK_INSIGHT_E2E_DATABASE_URL`, `STOCK_INSIGHT_E2E_SESSION_SECRET_PATH`, `STOCK_INSIGHT_E2E_USER_ID`, `STOCK_INSIGHT_E2E_USERNAME`, `STOCK_INSIGHT_E2E_PASSWORD`, and `PLAYWRIGHT_STORAGE_STATE` were also absent; downstream XG DB, build within release verification, and production/authenticated browser gates did not start. No live database was accessed.
- The review follow-up `graphify update .` completed with 9,495 nodes, 17,308 edges, and 645 communities. Existing graphify version and optional SQL parser warnings remain; ignored graph output is not staged.
- Review follow-up replaced the workspace-only forbidden-copy check with one reusable body-wide assertion, added `수익률`, and exercised it across every initial scenario plus default drawer/modal, observation, partial, and both detail-error retry states. A temporary `수익률` body mutation failed the focused desktop contract 1/1 before removal. The first combined GREEN exposed an existing close-transition race in the multi-entry test; waiting for inspector removal matched the established inspector E2E pattern and passed desktop/mobile 6/6 across three repeats.
- Controller-owned Codex in-app browser confirmation completed on isolated port 16150. At 1440×1000, the desktop drawer remained 520px wide without page overflow or source-layout movement, and the 760px modal preserved exact focus and selection. At 390×844, the bottom sheet was 390px wide, bottom-anchored, used a 16px top radius, and introduced no horizontal overflow. The controller also confirmed that `partial` kept failures localized and that `detail-error` retained selection before retrying successfully. This review's port 16151 runner is stopped; the controller-owned 16150 listener remains available for its handoff, and the user's 6100 server was never stopped or altered.
