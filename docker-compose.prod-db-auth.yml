# Post-enrollment production runtime. Static credentials and the enrollment verifier are intentionally absent.
name: stock-insight

services:
  api:
    # Pinned to an immutable digest, overridable per host. The default is the
    # last digest verified in production; a host that has built its own image
    # sets STOCK_INSIGHT_API_IMAGE in .env.docker instead of editing this file,
    # so a local build no longer leaves an uncommittable diff behind. Mirrors the
    # pattern docker-compose.edge.yml already uses.
    image: ${STOCK_INSIGHT_API_IMAGE:-sha256:8221ba3278bcfdee270c4062e6ea68190e9b137dccb08a1be79fa86947aba7c6}
    pull_policy: never
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: '6200'
      # The mutation kill-switch is enforced HERE now: P2 moved the manual
      # portfolio and personalization write paths into the brain, and
      # resolveManualPortfolioMutationPolicy() reads this from the brain's own
      # env. Leaving it unset makes every mutation fail closed with 503 while
      # reads keep succeeding.
      STOCK_INSIGHT_MUTATIONS_ENABLED: ${STOCK_INSIGHT_MUTATIONS_ENABLED:-true}
      DATABASE_READ_URL: ${STOCK_INSIGHT_DATABASE_READ_URL:?set in .env.docker}
      # The brain owns every write path too (manual portfolio, signup, invitation
      # issue/revoke) since the P2 split moved them off the BFF. Without this the
      # reads still succeed while every mutation fails closed with 503.
      DATABASE_WRITE_URL: ${STOCK_INSIGHT_DATABASE_WRITE_URL:?set in .env.docker}
      # Multi-user: no fixed server-owned id. The api-server verifies a signed
      # per-request internal context and refuses to boot without the secret.
      STOCK_INSIGHT_INTERNAL_CONTEXT_SECRET_FILE: /run/secrets/stock-insight-internal-context
    secrets:
      - stock-insight-internal-context
    ports:
      - '127.0.0.1:8093:6200'
    networks:
      # Joins the edge network so nginx can route insight-api.jigooo.com here.
      # The alias is what deploy/stock-edge/nginx.conf resolves.
      edge:
        aliases:
          - stock-insight-api
      research:
    init: true
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://127.0.0.1:6200/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 4s
      retries: 8
      start_period: 15s
    restart: unless-stopped

  app:
    image: ${STOCK_INSIGHT_APP_IMAGE:-sha256:eabac5cc656dc6cf7f2e313a33156b28882cac0d414b3587bd6db3fcc89cbc39}
    pull_policy: never
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: '3000'
      STOCK_INSIGHT_MUTATIONS_ENABLED: 'true'
      # The BFF holds NO database credentials. It reaches the brain in-network
      # over the edge alias, so this hop never leaves the Docker bridge and does
      # not traverse Cloudflare Access (that guards the public hostname, which is
      # for external developer clients).
      STOCK_INSIGHT_BRAIN_URL: ${STOCK_INSIGHT_BRAIN_URL:-http://stock-insight-api:6200}
      STOCK_INSIGHT_INTERNAL_CONTEXT_SECRET_FILE: /run/secrets/stock-insight-internal-context
      # Multi-user: signup mints users via invitation codes; no fixed user id.
      STOCK_INSIGHT_SIGNUP_ENABLED: ${STOCK_INSIGHT_SIGNUP_ENABLED:-true}
      STOCK_INSIGHT_SESSION_SECRET_FILE: /run/secrets/stock-insight-session-secret
      STOCK_INSIGHT_APP_ORIGIN: ${STOCK_INSIGHT_APP_ORIGIN:?set in .env.docker}
      STOCK_INSIGHT_SESSION_TTL_SECONDS: ${STOCK_INSIGHT_SESSION_TTL_SECONDS:-28800}
    secrets:
      - stock-insight-session-secret
      - stock-insight-internal-context
    ports:
      - '127.0.0.1:8091:3000'
    networks:
      edge:
        aliases:
          - stock-insight-app
    init: true
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://127.0.0.1:3000/api/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 4s
      retries: 8
      start_period: 20s
    restart: unless-stopped
    stop_grace_period: 15s

secrets:
  stock-insight-session-secret:
    file: ${STOCK_INSIGHT_SESSION_SECRET_HOST_PATH:?set in .env.docker}
  stock-insight-internal-context:
    file: ${STOCK_INSIGHT_INTERNAL_CONTEXT_SECRET_HOST_PATH:?set in .env.docker}

networks:
  edge:
    external: true
    name: stock-insight-edge
  research:
    external: true
    name: research-app-db_default
